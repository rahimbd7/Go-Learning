info:
  name: PATCH update a user
  type: http
  seq: 1

http:
  method: PUT
  url: "{{base_url}}/users/2"
  body:
    type: json
    data: |-
      {
        "name":"updated user",
        "age":40,
        "email":"update@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
