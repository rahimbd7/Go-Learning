info:
  name: Update user
  type: http
  seq: 3

http:
  method: PUT
  url: "{{Go_Base}}/users/3"
  body:
    type: json
    data: |-
      {
        "name":"Mishel",
        "age":25,
        "email":"mishel@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
