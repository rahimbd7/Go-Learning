info:
  name: GET user
  type: http
  seq: 2

http:
  method: GET
  url: "{{Go_Base}}/users"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
