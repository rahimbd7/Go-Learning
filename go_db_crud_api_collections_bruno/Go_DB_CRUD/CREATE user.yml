info:
  name: "CREATE user "
  type: http
  seq: 1

http:
  method: POST
  url: "{{Go_Base}}/users"
  body:
    type: json
    data: |-
      {
        "name":"john wick",
        "age":64,
        "email":"john@wick.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
