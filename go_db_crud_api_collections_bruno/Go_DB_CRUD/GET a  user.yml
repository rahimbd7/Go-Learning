info:
  name: GET a  user
  type: http
  seq: 4

http:
  method: GET
  url: "{{Go_Base}}/users/5"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
